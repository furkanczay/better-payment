{
  "name": "better-payment-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter better-payment build",
    "build:web": "pnpm --filter web build",
    "test": "pnpm --filter better-payment test",
    "release:patch": "pnpm --filter better-payment release:patch",
    "release:minor": "pnpm --filter better-payment release:minor",
    "release:major": "pnpm --filter better-payment release:major",
    "release:beta": "pnpm --filter better-payment release:beta"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
